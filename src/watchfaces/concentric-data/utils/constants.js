import {
  TEXT_CHAR_HEIGHT,
  TEXT_CHAR_WIDTH,
  TEXT_CHARS,
} from '../utils/textChars';

export const DATA = {
  radius: px(185),
  circleText: {
    charImages: TEXT_CHARS,
    imageWidth: TEXT_CHAR_WIDTH,
    imageHeight: TEXT_CHAR_HEIGHT,
  },
  arc: {
    width: px(5),
  },
};
